# render.yaml - for reference only (not used in manual Web Service deployment)
# Deploy this app on Render as a Web Service (Free plan) manually:
#
# Build Command : pip install -r requirements.txt
# Start Command : gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
# Python Version: 3.11.0
#
# Environment Variable to add manually:
#   DATABASE_URL = <your Neon/Supabase free PostgreSQL connection string>
